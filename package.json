{
  "name": "foresight",
  "version": "1.0.0",
  "private": true,
  "description": "Foresight workspace",
  "workspaces": ["packages/web"],
  "scripts": {
    "dev": "npm run dev -w packages/web",
    "build": "npm run build -w packages/web",
    "start": "npm run start -w packages/web",
    "lint": "npm run lint -w packages/web"
  }
}
